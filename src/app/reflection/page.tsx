import { ReflectionPage } from "@/components/reflection/reflection-page";

export default function ReflectionRoutePage() {
  return (
    <main className="min-h-[100svh] overflow-x-hidden bg-[#090A0F] text-white md:min-h-screen">
      <ReflectionPage />
    </main>
  );
}
